const Commission = (props) => (
    <div className={'d-flex justify-content-between align-items-center'}>
        <div className={'bg-primary p-2 rounded-circle'}>
            <p className={'m-0 text-light'}>G1</p>
        </div>
        <div>10000000</div>
    </div>
);

export default Commission;